[general]
name = "time"
version = "1.0.2"
description = "Provides functions related to time and date."

[build]
files = ["time.fix"]
objects = ["lib.o"]
preliminary_commands = [["make", "-s", "lib.o"]]

[build.test]
files = ["test.fix"]
